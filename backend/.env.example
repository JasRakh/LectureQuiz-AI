DATABASE_URL=postgresql://user:password@localhost:5432/lecturequiz
JWT_SECRET=super-secret-key
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000

# OpenAI: Whisper (audio → text) + gpt-4o-mini (bullet points & quiz JSON)
OPENAI_API_KEY=''

# Must match how the browser reaches the API (used in stored /uploads/... URLs)
API_PUBLIC_URL=http://localhost:4000

# Optional: directory for uploaded videos (relative to backend cwd)
# UPLOAD_DIR=uploads

